# MATLAB g-code (legacy)

Copy `gcodeConverter_final14.m` and related scripts from `genetic_layout_design/genetic_SHAPE/app/legacy_gcode_examples/` when ready to print.

**Input:** `.mat` files from `data/output/matlab/subject_{id}/` (same names as before: `InterconnectElectrodePaths.mat`, `HeadMesh.mat`, `Landmarks.mat`, `LandmarkNames.mat`).

**Output:** printer `.txt` g-code (gitignored).
